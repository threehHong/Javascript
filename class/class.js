/* 
Class
같은 키값을 가진 새로운 객체를 만들어 낼 수 있는 객체

규칙 
class명의 첫 글자는 대문자로 표시한다.
class 내부에 constructor() 함수를 사용한다.
new 클래스명(); 형태로 호출한다
아래와 같은 형태로 클래스를 만들고 호출한다.
*/

// 브라우저에게 엄격하게 에러를 검토하라고 하는 명령어
// 'use strict'

// class Person {
//     constructor(name, age) {
//         this.name = name;
//         this.age = age;
//     }
//     showInfo() {
//         console.log(`나의 이름은 ${this.name}이고, 나이는 ${this.age}입니다`);
//     }
// }

// const a = new Person('Tom', 22);
// const b = new Person('Jake', 25);

// console.log(a, b);

// console.log(a.showInfo());


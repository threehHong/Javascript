/* 
str.repeat(count);
주어진 문자열을 주어진 횟수만큼 반복해서 이어 붙인 문자열을 반환하는 메소드.

str - 문자열
count - 반복할 횟수
*/

let arr = "arr";
let result;

/* result = "abc".repeat(-1); */ // RangeError
/* result = arr.repeat(1); */ //
/* result = arr.repeat(3); */ //

console.log(result);
